pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				sh 'echo Building'
			}
		}
		stage('Test') {
			steps {
				sh 'echo Testing' // Use the sh step to execute shell commands
			}
		}
		stage('Deploy') {
			steps {
				sh 'echo Deploying'
			}
		}
	}
}
